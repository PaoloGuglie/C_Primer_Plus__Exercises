#include <stdio.h>

int main(void)
{
  const int NUMBER = 22;

  for (int count = 1; count <= NUMBER; count++)
  {
    printf("Number: %d\n", count);  // modified the exercise's output
  }

  return 0;
}